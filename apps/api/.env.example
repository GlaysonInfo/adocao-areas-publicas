# Ambiente
NODE_ENV=development

# Servidor
PORT=3001
HOST=0.0.0.0

# CORS (origens permitidas; separados por vírgula)
CORS_ORIGINS=http://localhost:5173

# Observabilidade / build info (opcional)
GIT_SHA=dev
