# apps/api — API (Fastify + TypeScript)

## Rodar local
```bash
cd apps/api
npm i
cp .env.example .env
npm run dev
```

## Endpoints
- `GET /health`
- `GET /version`
- Swagger UI: `GET /docs`

## Build & run
```bash
npm run build
npm start
```
